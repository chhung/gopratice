# lab1

This lab is a small standalone Go project that demonstrates simple operations behind a minimal application structure.

## Run

```bash
go run ./cmd/app
```

## Structure

- `cmd/app`: application entrypoint
- `internal/app`: application flow
- `internal/service`: reusable logic
- `internal/model`: domain types
- `test`: integration-test placeholder
